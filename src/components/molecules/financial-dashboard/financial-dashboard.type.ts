export type props = {
  icon: string;
  textTitle: string;
  textValue: string;
  financialHistory: string;
};
export type propsFinancialDashboardList = {
  financialDashboard: props[];
  className?: string;
};
